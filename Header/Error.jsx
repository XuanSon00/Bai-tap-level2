import { useSearchParams } from "react-router-dom";

export default function Error(){
    //let [searchParams, setSearchParams] = useSearchParams()
    //console.log(searchParams.get)


return <h1>Trang này không tìm thấy</h1>
}